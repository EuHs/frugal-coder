# Frugal Coder - 用户配置
# 通过管理后台 http://127.0.0.1:4020/ 修改

provider:
  # === Cheap 模型上游配置 ===
  # 即 sse-fix-proxy 的上游地址
  api_base: "https://www.tongxuanzn.icu/v1"
  api_key: "YJT1025zxy0427"
  model: "grok-4.1-fast"

gateway:
  port: 4010

# === 主模型配置（NEEDS_TOOLS 和 Direct 模式用）===
# 支持任意 OpenAI-compatible API
main:
  api_base: "https://api.minimaxi.com/anthropic"
  api_key: ""
  model: "minimax/MiniMax-M2.7"
