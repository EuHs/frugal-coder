# Frugal Coder 配置 - Grok2API 示例
# 复制为 config.yaml 并填入你的实际信息

provider:
  api_base: "https://YOUR_UPSTREAM_API/v1"
  api_key: "YOUR_API_KEY"
  model: "grok-4.1-fast"
  fix_sse: true
  strip_tools: true
  timeout: 180

gateway:
  host: "127.0.0.1"
  port: 4010
